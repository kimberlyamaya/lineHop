import React from 'react';
import { SafeAreaView, Text } from 'react-native';


const Account = () => {
  return (
    <SafeAreaView>
        <Text>Account</Text>
    </SafeAreaView>
  );
};

export default Account;